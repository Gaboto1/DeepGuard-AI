[build]
  command   = "npm run build"
  publish   = "out"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — todas las rutas sirven index.html (Next.js App Router)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
